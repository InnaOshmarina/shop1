import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Dashboard extends Component {
    render() {
        return (
            <div>
                There will be the information for the dashboard
            </div>
        );
    }
}

Dashboard.propTypes = {};

export default Dashboard;
